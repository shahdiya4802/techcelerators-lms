const leadSources = ['Meta', 'Instagram', 'Website', 'WhatsApp', 'Referral', 'Walk-in', 'FormCirculation'];
const connectivityStatuses = ['connected', 'dnp', 'vulgar', 'cut_call', 'switch_off', 'wrong_number', 'time_given'];
const currentStatuses = ['follow_up', 'dnp', 'denied', 'payment_done'];

module.exports = {
  leadSources,
  connectivityStatuses,
  currentStatuses,
};
